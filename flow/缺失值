缺失值分类+处理
完全变量：该样本数据中所有数据均完整的变量；
不完全变量：该样本数据中含有缺失值的变量。

类别：
1.随机缺失：该样本数据中缺失的数据与数据本身无关，与该样本数据中未缺失的数据有关，即该数据的缺失值依赖于其他完全变量；
2.完全随机缺失：该样本数据的缺失与其它值均无关；
3.非随机缺失：该样本缺失的数据与该样本中其它数据相关，分以下两种情况 
  3.1缺失值取决于其假设值：eg.高收入人群通常不希望在调查中透露他们的收入；
  3.2缺失值取决于该样本数据的其他变量值：eg.女性通常不想透露她们的年龄，则年龄变量缺失值受性别变量的影响。
1与2可根据具体情况对含有缺失值的数据进行删除处理，或者通过该缺失值变量的总体分布对缺失值进行填充。
3则要根据具体情况进行判断是删除还是填充，填充时要足够谨慎。


处理：
1.直接删除：适用于比例较小的情况，删除后对结果没有影响的情况
2.填充：
  2.1人工填充：具有主观臆断性（不推荐）
  2.2特殊值填充：用“unknown”来代替缺失值（没有意义，不推荐）
  2.3平均值填充：（该缺失属性的分布的大部分与平均值相近）
     2.3.1缺失值为数值型：根据该缺失值的属性，在总体中将该属性的平均值计算，然后用平均值进行填充；
     2.3.2缺失值为非数值型：该缺失属性，样本总体中出现最多的字符来填充。
  2.4条件平均值填充：（用于求平均的值并不是从数据集的所有对象中取，而是从与该对象具有相同决策属性值的对象中取得），然后思路与2.3相同，只是用于填充的属性发生了改变
  2.5热卡填充：（就近补齐）在样本数据中找一个与它最相似的属性，然后用这个相似属性的值来进行填充。（主观因素较多）
  2.6回归填充：（较为常用）（当变量不是线性相关时会有偏差）
     用不含缺失值的其它数据，建立回归方程，或利用机器学习中的回归算法。
     得到缺失值属性与其它属性之间的关系函数，将已知属性值代入关系函数来估算缺失值，从而进行填充。
  2.7K最近距离邻法：根据欧式距离或相关分析来确定距离具有缺失数据的样本最近的K个样本，将这K个值加权平均来估计该样本的缺失数据。
     先找K个邻居，在加权平均估计来填充数据。（KNN算法）
（KNN算法待补充）
未完待续
